/**
 * Types definition used on Cart screen
 *
 * @format
 * @flow
 */

// PropTypes for Cart screen Component
export type CartPropTypes = {
  items: Array<{}>,
  itemsCount: number,
  total: number,
};
