/**
 * Styles for ProductList screen
 *
 * @flow
 * @format
 * */
import colors from '../../config/colors';

export default {
  container: {
    backgroundColor: colors.background,
    flex: 1,
  },
};
